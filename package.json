{
  "name": "remote-ops-mcp",
  "version": "1.1.0",
  "description": "Remote read-only MCP operations gateway with durable OAuth, target revocation, audit and admin console.",
  "type": "module",
  "main": "dist/index.js",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js",
    "check": "npm run build && bash test-e2e.sh"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "ssh2": "^1.16.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "@types/ssh2": "^1.15.4",
    "typescript": "^5.7.2"
  }
}
